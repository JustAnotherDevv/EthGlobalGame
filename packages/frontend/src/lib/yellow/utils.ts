// Yellow Network constants and utilities

export const YELLOW_BROKER_URL = 'wss://clearnet-sandbox.yellow.com';

// Add utility functions here
export const formatYellowBalance = (balance: string) => {
  return balance; // Add formatting logic
};
